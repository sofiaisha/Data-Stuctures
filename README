Generic data structures in C:
- circular buffer (cbuffer.h)
- linked list (list.h), with multiple add modes : add first, add last, add sorted ascending, add sorted descending
- stack (stack.h)
- queue (queue.h)
- priority queue (pqueue.h), both in min and max configuration
- hashtable (hashtable.h)
- binary tree (binarytree.h)
- radix tree (radixtree.h), with customizable alphabet size

Unit-tests written with CUnit (*_test.[ch]).
Code checked with cppcheck and valgrind.
Code coverage with gcov.
